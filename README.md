#include <stdio.h>
int main()
{
    int A, B, sum;
    printf("Enter first number: ");
    scanf("%d", &A);
    printf("Enter second number: ");
    scanf("%d", &B);
    sum =A+B;
    printf("Sum = %d", sum);
    return 0;
}
